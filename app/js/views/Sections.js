const Sections = {
  INITIAL: 'initial',
  TRANSITION: 'transition',
  LOADER: 'loader',
  HOME: 'home',
  PRE_LANDING: 'pre_landing'
};

const SectionsURLs = {
  INITIAL: '/initial',
  TRANSITION: '/transition',
  LOADER: '/loader',
  HOME: '/',
  PRE_LANDING: '/pre-landing'
};

export { Sections, SectionsURLs };
