const Sections = {
  INITIAL: 'initial',
  TRANSITION: 'transition',
  TEST: 'test',
  LOADER: 'loader',
  HOME: 'home'
};

const SectionsURLs = {
  INITIAL: '/initial',
  TRANSITION: '/transition',
  TEST: '/test',
  LOADER: '/loader',
  HOME: '/'
};

export { Sections, SectionsURLs };
