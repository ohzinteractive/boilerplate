const Sections = {
  INITIAL: 'initial',
  TRANSITION: 'transition',
  LOADER: 'loader',
  HOME: 'home'
};

const SectionsURLs = {
  INITIAL: '/initial',
  TRANSITION: '/transition',
  LOADER: '/loader',
  HOME: '/'
};

export { Sections, SectionsURLs };
