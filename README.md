# OHZI Boilerplate

## Install
- `yarn install`
- `git submodule update --init`
- `cd core && git config core.hooksPath git_hooks`

## Usage
- `yarn start`

## Update submodule
- `git submodule update --remote`
