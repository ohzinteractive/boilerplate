# OHZI Boilerplate

## Requirements
- Node v14+

## 1. Set hooks
- `git config core.hooksPath git_hooks && cd core && git config core.hooksPath git_hooks && cd ..`

## 2. Install
- `yarn install`

## 3. Usage
- `yarn start`

## Update core submodule
- `git submodule update --remote`
